# Audit report
Run on: 2026-05-12
Scope: full
Time since last audit: first audit

## Summary

`system.md` is in very good shape — bootstrap was recent (same day), distillation is complete (0 TODO markers), all five invariants validated cleanly against code, and every file/directory reference in the cold doc resolves. Two low-priority flags only: one missing slash-command entry, and an untracked `lexicon/decisions/` folder. The signal in this audit is weak by design — there has been no time for backward-flow drift to accumulate. A re-audit in 4–6 weeks (or after the first crystallize) will be more informative.

## Glossary findings

- *Healthy*: 24 of 25 glossary entries validated against code by literal and structural grep. All file paths, module names, sidecar filenames, port `27182`, and protocol concepts (queryId, AsyncIterableController, compact boundary, touched/relevant notes, etc.) resolved cleanly.
- *Drifted name*: **Slash command** entry in `system.md:23` lists built-ins as `/new`, `/compact`, `/done`, `/rename`, `/sessions`. Actual `src/commands/builtins/` contains a sixth: `version.ts` (`/version`, alias `/v`). Either add `/version` to the glossary line or, if it's considered too trivial to enumerate, change the listing to "examples include …" so the doc isn't claiming exhaustiveness.

## Invariant findings

- *Holds*: all 5 invariants in `system.md` validated against current code:
  - `countUserOnlyMessages` ↔ `history.js` user-message counting both exclude `toolUseResult` (confirmed at `server/sessions.js:178` and `server/routes/history.js:114`).
  - `isDesktopOnly: false` in `manifest.json:8`; Node builtins behind try-catch guards in `src/main.ts:18-24`; `deploy.js` + `../deploy-plugin.js` confirmed mobile path receives only files (no `server/` `dirs` entry).
  - Auth token symmetric: `AUTH_TOKEN` env at `server/middleware/auth.js:12`, `Bearer` extraction at `server/middleware/auth.js:27-31`, single `remoteAuthToken` setting feeds both sides.
  - Touched-notes "latest op wins" + total `count` across ops: confirmed at `server/touchedNotes.js:88` ("promote to latest op") and JSDoc at `:18-20`.
  - Compact summaries cwd `/tmp/claude-agent-compact-summaries` declared and used in `server/transcript.js:7,85,95`.

## Bounded context findings

- All 5 contexts (Chat & Sessions, Embeddings & Retrieval, Semantic Graph, Connection & Transport, Flashcard integration) map cleanly to the code paths listed. `src/graph/` and `src/ui/GraphView/` and `src/ui/GraphView.tsx` all present as claimed. No new top-level modules outside named contexts; no obvious boundary leaks (boundaries are protocol-defined here — HTTP/SSE between plugin and proxy, plus window events for cross-plugin — both still intact).

## Hygiene

- `lexicon/retros/` count: 0 — expected for a same-day bootstrap; no flag.
- Last crystallization: never (no `.last-crystallized` file) — expected; no flag.
- Stale audit reports: none.
- **Untracked `lexicon/decisions/`**: the three ADR files (`ADR-0001` … `ADR-0003`) exist on disk but the whole `lexicon/decisions/` folder shows as `??` in `git status`. Either commit or add a deliberate `.gitignore` rule. Currently, the cold doc references these ADRs but they are not part of the repo history — anyone cloning will see a broken reference.
- `lexicon/bootstrap.md` deleted from git but still listed in `lex-overview`'s project-shape diagram. That's a `lex-overview` convention, not a per-project requirement — fine to leave deleted; just noting.
- `lexicon/system.md` itself has uncommitted modifications. Not unusual mid-session; flagging only as a reminder that this audit was run against the working-tree version, not the committed version.

## Prefs coherence

- `~/src/lexicon/lexicon-prefs.md` exists; all four sections empty. Expected for a project that has just been bootstrapped and has run zero retros — there's nothing to calibrate against yet. No flags.

## Distillation status

- TODO markers in `system.md`: 0.
- Distillation appears complete — post-bootstrap distillation session was run.

## Recommended actions, prioritized

1. **Add `/version` to the slash-command glossary entry** in `system.md:23`, or rephrase to "examples include …". Trivial edit; one-liner. Can route through the next `lex-crystallize` rather than a one-off direct edit.
2. **Commit `lexicon/decisions/`** (or add a deliberate exclusion). The ADRs are referenced from `system.md` but absent from version control; this will bite the first time the repo is cloned or audited from a fresh checkout.
3. **Defer the next audit ~4-6 weeks** or until the first `lex-crystallize` has run. Audit signal is currently weak because nothing has had time to drift; spending more tokens here right now is low-yield.

## Items deliberately not flagged

- The "active file is sent with each /chat request" wording in the **Active file** glossary entry — technically the request shape carries `activeFile` as one of several fields; checked code, claim holds; not a flag.
- The various design-system / region machinery in `lex-overview` and `lex-audit` Phase 1 — `system.md` has no `## Design system` section and no `lexicon/views/design-system.md`, so those phases are no-ops per spec. The plugin is a small React+Jotai UI without an explicit design-system layer; no flag.
- `OPENCLAW_REPLICATION_PLAN.md` — listed under "Things deliberately not specified" in `system.md:104`, and the spec was moved into `lexicon/plans/_archive/openclaw-memory/spec.md`. The acknowledged-as-deferred state is honest and not a flag.
- The empty `lexicon/retros/` folder — would be a flag at month-3, not at day-zero.
- All the modified-but-uncommitted source files outside `lexicon/` — outside audit scope.
